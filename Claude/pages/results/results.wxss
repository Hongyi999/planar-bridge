.results-page,
.results-page view,
.results-page scroll-view {
  box-sizing: border-box;
}

.results-page {
  min-height: 100vh;
  background: #F2EFE4;
  overflow-x: hidden;
  width: 100%;
}

/* Navigation */
.custom-nav {
  position: fixed;
  left: 0;
  right: 0;
  z-index: 100;
  display: flex;
  align-items: center;
  padding-left: 16rpx;
  background: #F2EFE4;
}

.nav-btn-group {
  display: flex;
  align-items: center;
  background: rgba(253, 252, 248, 0.85);
  border: 1rpx solid #D6D0C2;
  overflow: hidden;
  z-index: 2;
  backdrop-filter: blur(10px);
}

.nav-btn {
  display: flex;
  align-items: center;
  justify-content: center;
}

.nav-btn:active {
  background: rgba(235, 231, 218, 0.6);
}

.nav-divider {
  width: 1rpx;
  height: 50%;
  background: #D6D0C2;
}

/* Back arrow — clean chevron */
.nav-arrow-back {
  width: 18rpx;
  height: 18rpx;
  border-left: 3rpx solid #555249;
  border-bottom: 3rpx solid #555249;
  transform: rotate(45deg);
  margin-left: 4rpx;
  border-radius: 1rpx;
}

/* Home icon — house shape */
.nav-home-icon {
  position: relative;
  width: 30rpx;
  height: 28rpx;
}

.home-base {
  position: absolute;
  bottom: 0;
  left: 4rpx;
  width: 22rpx;
  height: 14rpx;
  border: 2.5rpx solid #555249;
  border-top: none;
  border-radius: 0 0 4rpx 4rpx;
  background: transparent;
}

.home-roof-line {
  position: absolute;
  top: 0;
  left: 50%;
  transform: translateX(-50%);
  width: 0;
  height: 0;
  border-left: 17rpx solid transparent;
  border-right: 17rpx solid transparent;
  border-bottom: 13rpx solid #555249;
}

.nav-title {
  position: absolute;
  left: 50%;
  transform: translateX(-50%);
  font-size: 28rpx;
  font-weight: 600;
  color: #2C2A22;
  letter-spacing: 0.04em;
}

/* Content */
.results-scroll {
  height: 100vh;
  padding: 0 28rpx;
}

.search-compact {
  margin-bottom: 32rpx;
  padding-top: 24rpx;
}

.thinking-section {
  margin-bottom: 24rpx;
}

/* Summary */
.summary-card {
  background: #FDFCF8;
  border: 3rpx solid #9B864420;
  border-radius: 32rpx;
  padding: 28rpx;
  margin-bottom: 24rpx;
  opacity: 0;
  transform: translateY(20rpx);
  transition: opacity 0.5s ease-out, transform 0.5s ease-out;
}

.summary-card.show {
  opacity: 1;
  transform: translateY(0);
}

.summary-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 16rpx;
}

.summary-label {
  font-size: 22rpx;
  color: #A09A8C;
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.summary-count {
  font-size: 24rpx;
  color: #9B8644;
  font-weight: 600;
}

.summary-text {
  font-size: 25rpx;
  color: #4A4636;
  line-height: 1.6;
}

/* Results meta */
.results-meta {
  display: flex;
  justify-content: space-between;
  padding: 0 8rpx;
  margin-bottom: 20rpx;
  opacity: 0;
  transition: opacity 0.4s ease-out;
}

.results-meta.show {
  opacity: 1;
}

.meta-left {
  font-size: 22rpx;
  color: #A09A8C;
}

.meta-right {
  font-size: 22rpx;
  color: #6B4C8A;
  font-weight: 500;
}

/* Card grid */
.card-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 20rpx;
  opacity: 0;
  transition: opacity 0.4s ease-out;
}

.card-grid.show {
  opacity: 1;
}

/* Empty state */
.empty-state {
  text-align: center;
  padding: 80rpx 40rpx;
  opacity: 0;
  transition: opacity 0.4s;
}

.empty-state.show {
  opacity: 1;
}

.empty-text {
  display: block;
  font-family: Georgia, "Songti SC", serif;
  font-size: 32rpx;
  color: #6B6355;
  margin-bottom: 12rpx;
}

.empty-hint {
  font-size: 24rpx;
  color: #A09A8C;
}

.bottom-spacer {
  height: 60rpx;
}
